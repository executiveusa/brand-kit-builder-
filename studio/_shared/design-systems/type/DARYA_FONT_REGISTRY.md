# DARYA Font Registry — migration pointer

**Canonical content is temporarily still at:** `../../../../DARYA_FONT_REGISTRY.md`

This pointer exists so new ICM routes resolve during Phase 1 without duplicating the full typography registry. Before the Phase 1 migration PR is merged, the registry content must be moved here and the legacy root file reduced to a pointer.

Do not edit typography facts in this pointer. Until the move is completed, edit the root registry only.
