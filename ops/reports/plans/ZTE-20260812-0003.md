# ZTE-20260812-0003 — Phase 2 Human Design Office

## Objective
Turn the Phase 0 static homepage prototype into the first real application shell for the Pauli Brand Studio: outcome-first, proof-first, chat-centered, accessible, responsive, and explicit about what is live versus planned.

## Memory/repo synthesis
- Root routing is now canonical through `AGENTS.md` → `CONTEXT.md` → `studio/CONTEXT.md`.
- Human surface contract is `apps/web/CONTEXT.md`.
- REST contract lives at `interfaces/rest/API.md`; Popebot is an adapter boundary under `interfaces/popebot/` and does not own brand truth.
- The existing homepage prototype is the approved visual/product starting point and includes the Antoine de Saint-Exupéry hero quote.
- The uploaded Design OS donor is MIT-licensed, Vite + React 19 + TypeScript + Tailwind 4 + Radix + Lucide. It is a donor, not the product architecture. Its dashboard/card-heavy visual language is not copied wholesale.

## Files to create/modify
- `apps/web/package.json`
- `apps/web/index.html`
- `apps/web/tsconfig*.json`
- `apps/web/vite.config.ts`
- `apps/web/eslint.config.js`
- `apps/web/src/main.tsx`
- `apps/web/src/App.tsx`
- `apps/web/src/styles.css`
- focused components for navigation, proof hero, outcome composer, work proof, system/ownership explanation, and status truth labels
- `apps/web/NOTICE.md` for Design OS MIT attribution if donor implementation code is adapted
- update `apps/web/CONTEXT.md` from prototype-only to application-shell truth
- update demo-brand-book reference path only when the rendering move is done in the same slice

## UX contract
Primary journey:
`Home / proof → Ask for an outcome → Route preview / job state → Project proof → Decision gate → Export`

Phase 2 implements the **human shell and local deterministic prototype behavior only**. It does not fake Popebot, Supabase, MCP, CLI, agent execution, or a running factory.

## Validation criteria — binary
- [ ] `npm run build` succeeds.
- [ ] `npm run lint` succeeds.
- [ ] TypeScript build succeeds.
- [ ] No runtime secrets or production endpoints are hardcoded.
- [ ] Hero quote remains present and attributed.
- [ ] Current prototype proof content is preserved or improved without fake claims.
- [ ] Composer is explicitly truth-labeled until runtime wiring exists.
- [ ] Keyboard-visible focus, semantic landmarks, skip link and reduced-motion behavior exist.
- [ ] Mobile layout keeps essential context.
- [ ] Design OS attribution is preserved for any copied/substantially adapted MIT code.
- [ ] Branch diff contains no Supabase or production deployment mutation.

## Rollback
Revert the Phase 2 PR. Phase 1 ICM structure remains intact.

## Risk
**MEDIUM.** New application runtime and build tooling, but no production deployment, database mutation, or secret change.
