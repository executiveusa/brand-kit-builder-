# ZTE-20260811-0002 — Phase 1 ICM migration plan

## Objective
Physically reorganize the Pauli Brand Studio brownfield repository into the approved ICM v2 structure without breaking legacy routes or claiming runtime capabilities that do not exist.

## Scope
- Move core factory law into `studio/_system/`.
- Move reusable creative intelligence into `studio/_shared/`.
- Move REST contract into `interfaces/rest/` and create truthful adapter contexts for planned interfaces.
- Create `studio/projects/`, `studio/_templates/`, `portability/`, `supabase/`, and `vercel/` context boundaries.
- Reduce migrated legacy files to compatibility pointers so there is one editable home per fact.
- Update root/studio cold-start routers.
- Record the PAULI'S PLACE downstream boundary.

## Explicitly deferred
- `demo-brand-book.html` path move, because it is a rendering/reference artifact tied to the Phase 2 app/runtime link rewrite.
- React/Vite app integration.
- Popebot runtime integration.
- Supabase schema application.
- Vercel deployment.
- Production changes.

## Validation criteria — binary
- [ ] Root `AGENTS.md` routes to canonical ICM paths.
- [ ] Root `CONTEXT.md` points to canonical ICM stage law.
- [ ] `studio/CONTEXT.md` routes factory/shared/project/template areas.
- [ ] Core migrated files exist at canonical target paths.
- [ ] Legacy core files contain pointers rather than duplicate truth.
- [ ] DARYA/KAKU/master prompt have one canonical editable home.
- [ ] REST/CLI/MCP/folder-drop/Popebot boundaries are explicit and truth-labeled.
- [ ] `compare_commits(main, phase1)` contains no unexpected production/runtime changes.
- [ ] GitHub status/CI state is inspected and reported accurately.

## Rollback
Revert the Phase 1 PR merge. Legacy pointer files and canonical new paths are introduced together in one branch, so a single PR revert restores the Phase 0 layout.

## Risk
**MEDIUM** — brownfield path migration with compatibility pointers. No database mutation, deployment, secret change, or irreversible operation.
