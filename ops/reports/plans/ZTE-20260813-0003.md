# ZTE-20260813-0003 — Phase 6 Portable Intelligence

## Objective
Export a self-describing `.pauli` project pack that remains understandable without the web app, Supabase, prior chat history, or a specific model provider.

## Scope
- project-level AGENTS/CONTEXT routers;
- stage folders and ledger copy;
- manifest/asset inventory with SHA-256 hashes;
- portability metadata and capability requirements without secret values;
- secret-pattern rejection before export;
- integrity verification on re-import;
- cold-start summary + next-task resolver from files only;
- Hermes-compatible mount documentation.

## Binary validation
- exporter rejects common secret material;
- exported pack contains no absolute source path or secret value;
- all indexed files verify by SHA-256;
- cold reader identifies tenant/project/current stage/protected items/hard bans/next permitted action from pack alone;
- tampering fails integrity verification;
- test performs no network/database access.

## Rollback
Revert Phase 6 PR. No cloud mutation.

## Risk
LOW/MEDIUM — packaging and file interpretation only.
