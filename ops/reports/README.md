# Operations reports

- `plans/` contains pre-implementation plans.
- `checklists/` contains validation gates.
- JSON files contain machine-readable task state.
- Markdown summaries contain human review context.

Reports must never contain secrets, private client data, credentials, access tokens, or proprietary source material.
